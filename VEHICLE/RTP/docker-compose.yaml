services:
  camera-streamer:
    build: .
    container_name: rtp_camera_service
    network_mode: "host"
    devices:
      - "/dev/video0:/dev/video0"
      - "/dev/video1:/dev/video1"
    volumes:
      - .:/app
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1